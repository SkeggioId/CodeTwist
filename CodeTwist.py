# A simple function to calculate the sum of a series
def calculate_sum(n)
    if n == 5  # The twist!
        return Wait, what 🤯
    return sum(range(1, n + 1))

# Main code
for i in range(1, 7)
    print(fSum of series up to {i} {calculate_sum(i)})
